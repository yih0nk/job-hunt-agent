# Personal data — never committed. Only the *.example.yaml templates are public.
config/profile.yaml
config/preferences.yaml
config/learned-answers.yaml

# Runtime state (seen-role hashes, queue, drafted packages)
state/*
!state/.gitkeep

# Drafted application packages (contain tailored resumes + personal answers)
applications/

# OS / editor
.DS_Store
*.pyc
__pycache__/
.venv/
